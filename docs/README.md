# Web trip planner

Static frontend in `docs/` + Vercel serverless `/api/japanese`.

## Secrets
- GitHub Actions: `NVIDIA_API_KEY`, optionally `VERCEL_TOKEN`, `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID`
- Vercel project env: `NVIDIA_API_KEY` (runtime for `/api/japanese`)
